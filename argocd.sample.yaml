upstream:
  backstage:
    appConfig:
      argocd:
        appLocatorMethods:
          - type: 'config'
            instances:
              - name: instanceA
                url: '${ARGOCD_SERVER_URL}'
                username: admin
                password: '${ARGOCD_ADMIN_PASSWORD}'
